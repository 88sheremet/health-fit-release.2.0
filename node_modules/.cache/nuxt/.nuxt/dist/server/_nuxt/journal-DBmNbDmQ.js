import { defineStore } from "pinia";
//#region app/stores/journal.ts
var useJournalStore = defineStore("journal", {
	state: () => ({
		entries: [],
		showCheckin: false
	}),
	getters: {
		chartData(state) {
			return state.entries.slice().sort((a, b) => new Date(a.date).getTime() - new Date(b.date).getTime()).map((entry, index) => ({
				day: index + 1,
				mood: entry.mood,
				date: entry.date,
				note: entry.note
			}));
		},
		lastEntry(state) {
			if (!state.entries.length) return null;
			return state.entries[state.entries.length - 1];
		}
	},
	actions: {
		init() {
			const today = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
			const lastCheckinDate = localStorage.getItem("last-checkin-date");
			this.showCheckin = lastCheckinDate !== today;
		},
		saveCheckin(payload) {
			const today = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
			const existingEntry = this.entries.find((entry) => entry.date === today);
			if (existingEntry) {
				existingEntry.mood = payload.mood;
				existingEntry.note = payload.note;
			} else this.entries.push({
				id: crypto.randomUUID(),
				date: today,
				mood: payload.mood,
				note: payload.note
			});
			localStorage.setItem("last-checkin-date", today);
			this.showCheckin = false;
		},
		closeCheckin() {
			this.showCheckin = false;
		},
		getEntryByDate(date) {
			return this.entries.find((entry) => entry.date === date);
		},
		deleteEntry(id) {
			this.entries = this.entries.filter((entry) => entry.id !== id);
		},
		addNote(note) {
			const today = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
			this.entries.push({
				id: crypto.randomUUID(),
				date: today,
				note
			});
		}
	},
	persist: true
});
//#endregion
export { useJournalStore as t };

//# sourceMappingURL=journal-DBmNbDmQ.js.map